angular.module("fluro.socket",[]),angular.module("fluro.socket").service("FluroSocket",function(Fluro,$rootScope){var socket,currentSocketID,controller={},host=Fluro.apiURL,currentAccount="";return"undefined"!=typeof io&&(socket=io(host),$rootScope.$watch("user.account._id",function(){var user=$rootScope.user;user?user.account&&user.account._id?(currentAccount=user.account._id,controller.join(currentAccount)):controller.leave(currentAccount):currentAccount&&controller.leave(currentAccount)})),controller.getSocketID=function(){return currentSocketID},controller.join=function(roomName){socket&&(console.log("join",roomName),socket.on("connect",function(){currentSocketID=socket.io.engine.id,socket.emit("subscribe",{room:roomName})}),socket.on("reconnect",function(){currentSocketID=socket.io.engine.id,socket.emit("subscribe",{room:roomName})}),socket.on("disconnect",function(){currentSocketID=null}))},controller.leave=function(roomName){socket&&(console.log("leave",roomName),socket.emit("unsubscribe",{room:roomName}),socket.off("connect"),socket.off("reconnect"),socket.off("disconnect"))},controller.emit=function(roomName,key,data){socket.to(roomName).emit(key,data)},controller.on=function(event,callback){socket&&socket.on(event,callback)},controller.off=function(event,callback){socket&&socket.off(event,callback)},controller});